# Library API

1. Copy `.env.example` to `.env` and set `DATABASE_URL`.
2. Create the PostgreSQL database named `library_manager`.
3. Run `npm install`, `npx prisma generate`, and `npx prisma migrate dev --name init`.
4. Start with `npm run dev`.
